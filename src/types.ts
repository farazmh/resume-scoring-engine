export type ResumeProfile = {
  yearsOfExperience: number;
  skills: string[];
  targetRole: string;
};

export type ScoreBreakdown = {
  experience: number;
  skills: number;
  roleMatch: number;
};
